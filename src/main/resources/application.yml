server:
  servlet:
    context-path: /api/v1
spring:
  application:
    name: soccer
  #Configuraci�n BD
  datasource:
    url: jdbc:mysql://localhost:3306/soccerhub
    username: root
    password: root
  #Configuraci�n de Hibernate
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true